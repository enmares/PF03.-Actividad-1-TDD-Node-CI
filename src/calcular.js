
function esPinValido(pin){
    
    let resMal = -1;

    if(pin == null || pin == undefined || pin.length == 0 || (pin.length >= 4 && pin.length <=6) || !Number.isNaN(pin) || ){
        return resMal;
    }
}

function comprobarDigitosIguales(string){

    for(let i=0; )
}